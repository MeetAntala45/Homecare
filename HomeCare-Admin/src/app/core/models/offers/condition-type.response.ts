export interface IConditionTypeResponse {
    id: number;
    label: string;
    contextKey: string;
    inputType: string;
    defaultOperator: string;
    defaultFailBehaviour: string;
    isActive: boolean;
  }